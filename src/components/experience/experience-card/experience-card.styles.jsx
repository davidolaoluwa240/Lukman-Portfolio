// Modules
import styled from "styled-components";

export const ExperienceCardWrapper = styled.li`
  a {
    font-family: Studio Feixen Sans TRIAL;
    font-size: 1.8rem;
    font-weight: 800;
    line-height: 27px;
  }

  p:first-of-type {
    color: rgba(33, 33, 33, 0.7);
    font-weight: 400;
  }

  p:last-of-type {
    color: rgba(33, 33, 33, 0.5);
    margin-top: 0.35rem;
    font-size: 1.6rem;
    font-weight: 400;
    line-height: 24px;
    letter-spacing: -0.4px;
  }
`;
