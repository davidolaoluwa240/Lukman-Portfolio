// Modules
import styled from "styled-components";

export const ExperiencePreviewCardWrapper = styled.div`
  p {
    font-weight: 400;
  }

  p:first-child {
    color: var(--dark-gray);
  }

  p:last-child {
    color: rgba(33, 33, 33, 0.7);
    margin-top: 0.4rem;
    line-height: 28px;
  }
`;
